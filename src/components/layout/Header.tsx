import React from 'react';

const Header = () => {
  return (
    <header
      className={`h-header max-mobile:h-mobileHeader bg-red-200 flex justify-between items-center `}>
      header
    </header>
  );
};

export default Header;
